% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper-functions.R
\name{simulate_pca_data}
\alias{simulate_pca_data}
\title{Simulate Structured Data from a PCA Model}
\usage{
simulate_pca_data(n, p, k, noise_sd = 0.1, seed = NULL)
}
\arguments{
\item{n}{An integer specifying the number of observations (rows) to generate.}

\item{p}{An integer specifying the number of features (columns) in the dataset.}

\item{k}{An integer specifying the number of true latent components (rank of the PCA model).}

\item{noise_sd}{A numeric value specifying the standard deviation of Gaussian noise
to add to the data. Defaults to 0.1.}

\item{seed}{An optional integer for setting the random seed to ensure reproducibility.
Defaults to NULL.}
}
\value{
A numeric matrix of dimensions `n` x `p` containing the simulated data.
}
\description{
This function generates structured data by simulating latent factors and loadings
from a PCA model, optionally adding Gaussian noise. The resulting data can be used
to test dimensionality reduction and latent representation methods.
}
\examples{
# Simulate data with 100 observations, 10 features, and 3 latent components
data <- simulate_pca_data(n = 100, p = 10, k = 3, noise_sd = 0.1, seed = 42)

# Plot the first two features
plot(data[, 1], data[, 2], main = "Scatterplot of First Two Features")

}
